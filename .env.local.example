# ============================================================
# AI Disruption Tracker – Environment Variables
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.
# ============================================================

# ── Database ─────────────────────────────────────────────────
# Neon PostgreSQL connection string (or any PostgreSQL URL)
# Get yours at: https://neon.tech (free tier available)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# ── Twitter / X API v2 ───────────────────────────────────────
# Create a project + app at: https://developer.twitter.com
# Requires "Essential" access (free) at minimum.
# The "Read Tweets" permission is sufficient.
TWITTER_BEARER_TOKEN=AAAAAAAAAAAAAAAAAAAAAxxxxxx...

# ── NewsAPI ──────────────────────────────────────────────────
# Register at: https://newsapi.org (free Developer plan)
# Note: free plan only works on localhost; upgrade for production.
# Alternative: use GNews (https://gnews.io) – set GNEWS_API_KEY instead.
NEWS_API_KEY=your_newsapi_key_here

# Optional: GNews as an alternative news source
# GNEWS_API_KEY=your_gnews_key_here

# ── Security ─────────────────────────────────────────────────
# Random secret used to authenticate Vercel Cron calls.
# Generate with: openssl rand -hex 32
CRON_SECRET=replace_with_random_32_char_hex

# Secret for the /admin dashboard API routes.
# Leave empty to allow unauthenticated access (dev only).
ADMIN_SECRET=replace_with_another_random_secret

# ── App config ───────────────────────────────────────────────
# Full URL of your deployment (no trailing slash)
NEXT_PUBLIC_BASE_URL=https://your-app.vercel.app
